# JUST_TEST_LAH configuration
# Check https://justtestlah.qa for a detailed documentation 

# GENERAL settings
platform=web
pages.package=${groupId}.pages
steps.package=${groupId}.steps
model.package=${groupId}.model
testdata.enabled=true
features.directory=src/test/resources/features
cucumber.report.directory=target/report/cucumber
cloudprovider=local
#if (${galen} == 'true' || ${galen} == 'yes' || ${galen} == 'y')
galen.enabled=true
#end

# WEB settings
web.baseUrl=https://www.google.com
web.browser=chrome
#if (${headless} == 'true' || ${headless} == 'yes' || ${headless} == 'y')
web.headless=true
#else
web.headless=false
#end
